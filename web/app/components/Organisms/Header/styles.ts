import styled from 'styled-components'

export const Wrapper = styled.header({
  padding: '16px 24px',
})

export const Title = styled.h1({
  margin: '0',
  fontSize: '20px',
  textAlign: 'center',
})
